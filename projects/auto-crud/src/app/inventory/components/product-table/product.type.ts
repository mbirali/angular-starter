// Define a type for the column names
export type Column = 'id' | 'name' | 'category' | 'price' | 'stock';

// Define an array of columns (optional)
export const COLUMN_NAMES: Column[] = ['id', 'name', 'category', 'price', 'stock'];
